import { atom } from 'jotai'

export const sidebarOpenAtom = atom(false);

export const modalOpenAtom = atom(false);

export const sectionDataAtom = atom({});

export const loaderRefAtom = atom(null);